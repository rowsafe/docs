---
title: Introduction
description: Rowsafe backs up the databases you run on your own servers, lets you restore to any second, and proves every week that your backups restore.
---

Rowsafe protects the databases you already run, on your own servers or in Docker. It takes backups, keeps a continuous record of every change, and restores your latest backup every week to prove it works. If something goes wrong, you can get your data back to the second before it happened.

You keep your database where it is. You don't move it to a hosted service.

## What you get

<Cards>
  <Card title="Rewind" href="/docs/guides/restore">
    Every change saved. Restore to any second, or to a Mark you set before a risky change.
  </Card>
  <Card title="Proof" href="/docs/concepts/restore-drills">
    Every week Rowsafe restores a copy of your database and checks it, so you know restores work before you need one.
  </Card>
  <Card title="Pulse" href="/docs/guides/monitoring">
    A health score, alerts to email, Slack, Discord or a webhook, and a weekly email about each database.
  </Card>
  <Card title="Guard" href="/docs/guides/ai-agents">
    A safety net for AI coding agents: they check that the database is safe and set a Mark before a migration.
  </Card>
</Cards>

Backups go to **your own storage bucket**, encrypted on your server. Setup is one command on the server.

## How it works

```text
You: dashboard, CLI, AI agents
        │
        │ HTTPS
        ▼
┌─────────────────────────┐
│ Rowsafe service         │
│ schedules, history,     │
│ alerts                  │
└─────────────────────────┘
        ▲
        │ HTTPS, outbound only
        │ (the agent asks for work)
┌─────────────────────────┐
│ Your server             │
│ agent ──▶ your database │
└─────────────────────────┘
        │
        │ encrypted backups
        ▼
Your S3-compatible bucket
```

1. A small open-source **agent** runs next to your database. It only makes outbound HTTPS requests. Nothing on your server listens for Rowsafe.
2. The agent encrypts backups **on your server** and sends them to **your own bucket**. Rowsafe never sees your data, your bucket keys or your encryption passphrase.
3. The hosted **Rowsafe service** schedules backups and the weekly Proof, keeps the history, watches for problems and alerts you.

Read [How Rowsafe works](/docs/concepts/how-it-works) for the details.

## Supported databases

| Database | Status | Where |
|---|---|---|
| PostgreSQL 13 to 18 | **Supported** | Linux servers (Debian 12/13, Ubuntu 22.04/24.04) and Docker (official `postgres` images, 14 to 18) |
| MySQL | Planned | |
| MariaDB | Planned | |
| MongoDB 6.0, 7.0, 8.0 | **Supported** | Replica sets (one member is enough) on Linux servers and in Docker (official `mongo` images). Not sharded clusters yet. See [Protect MongoDB](/docs/guides/mongodb) |
| Redis / Valkey | Planned | |

Planned engines are not available yet, and we don't give dates. The rest of these docs describe PostgreSQL; [Protect MongoDB](/docs/guides/mongodb) covers what is different for MongoDB.

## Safety first

Rowsafe works on production databases, so it is careful by design:

- **It never restarts your database on its own.** When a setting needs a restart, Rowsafe asks first. Say no, and you restart when it suits you.
- **Nothing changes until you say yes.** Rowsafe first shows, in plain words, what it will change.
- **Proof is isolated.** The weekly restore test restores into a scratch directory on a private socket, then deletes it. It can't touch production or its backups.
- **Your secrets stay on your server.** Backups are encrypted before they leave it.
- **Signed releases.** The agent only installs and runs releases signed by Rowsafe, and rolls back a bad update by itself.

## Next steps

<Cards>
  <Card title="Quickstart" href="/docs/quickstart">
    Protect your first database with one command, in about 10 minutes.
  </Card>
  <Card title="Adopt a production database" href="/docs/guides/adopt">
    The careful, step-by-step version for a server that matters, and automation.
  </Card>
  <Card title="Restore a database" href="/docs/guides/restore">
    Rewind: get your data back to a point in time or to a Mark.
  </Card>
  <Card title="Use Rowsafe with AI agents" href="/docs/guides/ai-agents">
    Guard: a Mark before every migration an agent runs.
  </Card>
</Cards>
